# Структура базы данных Anki_Database (Google Sheets)

Скопировано из `database_schema.md`. См. оригинал в корне или используйте этот файл для ссылок внутри репозитория.

ID Таблицы и название листа указываются в `docs/ARCHITECTURE.md`.

Спецификация колонок и правила заполнения описаны в `database_schema.md` (источник).
